// All styles are imported to a central index file, and are exported at the end. This allows for all of the exported styles are able to be imported from a single file.


import { StyledCell } from "./StyledCell";
import {StyledTetrisWrapper, StyledTetris} from './StyledTetris';
import {StyledStage} from './StyledStage';
import { StyledDisplay } from "./StyledDisplay";
import { StyledStartButton } from "./StyledStartButton";





export {StyledCell, StyledTetrisWrapper, StyledTetris, StyledStage, StyledDisplay, StyledStartButton }